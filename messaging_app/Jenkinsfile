pipeline {pipeline {

    agent any    agent any



    environment {    environment {

        // GitHub repository details        // GitHub repository details

        REPO_URL = 'https://github.com/FredrickMbithi/alx-backend-python.git'        REPO_URL = 'https://github.com/FredrickMbithi/alx-backend-python.git'

        BRANCH = 'main'        BRANCH = 'main'

                

        // Python settings        // Python settings

        PYTHON_VERSION = '3.10'        PYTHON_VERSION = '3.10'

                

        // MySQL settings for tests        // MySQL settings for tests

        DATABASE_HOST = '127.0.0.1'        DATABASE_HOST = '127.0.0.1'

        DATABASE_PORT = '3306'        DATABASE_PORT = '3306'

        DATABASE_NAME = 'test_messaging_db'        DATABASE_NAME = 'test_messaging_db'

        DATABASE_USER = 'root'        DATABASE_USER = 'root'

        DATABASE_PASSWORD = 'root'        DATABASE_PASSWORD = 'root'

    }    }



    stages {    stages {

        stage('Checkout') {        stage('Checkout') {

            steps {            steps {

                echo '📦 Checking out code from GitHub...'                echo '📦 Checking out code from GitHub...'

                // Checkout code from GitHub using credentials                // Checkout code from GitHub using credentials

                git branch: "${BRANCH}",                 git branch: "${BRANCH}",

                    credentialsId: 'github-credentials',                     credentialsId: 'github-credentials',

                    url: "${REPO_URL}"                    url: "${REPO_URL}"

                                

                echo '✅ Code checked out successfully'                echo '✅ Code checked out successfully'

            }            }

        }        }



        stage('Setup Python Environment') {        stage('Setup Python Environment') {

            steps {            steps {

                echo '🐍 Setting up Python environment...'                echo '🐍 Setting up Python environment...'

                dir('messaging_app') {                dir('messaging_app') {

                    sh '''                    sh '''

                        echo "Python version:"                        echo "Python version:"

                        python3 --version                        python3 --version

                                                

                        echo "Installing dependencies..."                        echo "Installing dependencies..."

                        python3 -m pip install --upgrade pip                        python3 -m pip install --upgrade pip

                                                

                        if [ -f requirements.txt ]; then                        if [ -f requirements.txt ]; then

                            pip3 install -r requirements.txt                            pip3 install -r requirements.txt

                        else                        else

                            echo "⚠️  requirements.txt not found"                            echo "⚠️  requirements.txt not found"

                            exit 1                            exit 1

                        fi                        fi

                        

                        echo "Installing test dependencies..."                        echo "Installing test dependencies..."                echo 'Creating Python virtual environment...'

                        pip3 install pytest pytest-django pytest-cov pytest-html

                                                pip3 install pytest pytest-django pytest-cov pytest-html                dir('messaging_app') {

                        echo "Installed packages:"

                        pip3 list                                            sh '''

                    '''

                }                        echo "Installed packages:"                        ${PYTHON} -m venv ${VENV_DIR}

                echo '✅ Python environment ready'

            }                        pip3 list                        . ${VENV_DIR}/bin/activate

        }

                    '''                        pip install --upgrade pip setuptools wheel

        stage('Run Tests') {

            steps {                }                    '''

                echo '🧪 Running tests with pytest...'

                dir('messaging_app') {                echo '✅ Python environment ready'                }

                    sh '''

                        # Create reports directory            }            }

                        mkdir -p reports

                                }        }

                        # Run pytest with coverage and HTML report

                        python3 -m pytest \        

                            --verbose \

                            --tb=short \        stage('Run Tests') {        stage('Install Python Dependencies') {

                            --junitxml=reports/junit.xml \

                            --html=reports/report.html \            steps {            steps {

                            --self-contained-html \

                            --cov=. \                echo '🧪 Running tests with pytest...'                echo 'Installing Python packages...'

                            --cov-report=xml:reports/coverage.xml \

                            --cov-report=html:reports/htmlcov \                dir('messaging_app') {                dir('messaging_app') {

                            --cov-report=term-missing \

                            || true                    sh '''                    sh '''

                        

                        echo "Test execution completed"                        # Create reports directory                        . ${VENV_DIR}/bin/activate

                    '''

                }                        mkdir -p reports                        

                echo '✅ Tests completed'

            }                                                if [ -f requirements.txt ]; then

        }

                        # Run pytest with coverage and HTML report                            echo "Installing from requirements.txt..."

        stage('Generate Test Report') {

            steps {                        python3 -m pytest \                            pip install -r requirements.txt

                echo '📊 Generating test reports...'

                dir('messaging_app') {                            --verbose \                        else

                    sh '''

                        if [ -f reports/junit.xml ]; then                            --tb=short \                            echo "No requirements.txt found, installing basic deps"

                            echo "✅ JUnit XML report generated"

                            ls -lh reports/junit.xml                            --junitxml=reports/junit.xml \                            pip install Django pytest pytest-django

                        else

                            echo "⚠️  JUnit XML report not found"                            --html=reports/report.html \                        fi

                        fi

                                                    --self-contained-html \                        

                        if [ -f reports/report.html ]; then

                            echo "✅ HTML report generated"                            --cov=. \                        echo "Installing test dependencies..."

                            ls -lh reports/report.html

                        else                            --cov-report=xml:reports/coverage.xml \                        pip install --upgrade pytest pytest-django parameterized

                            echo "⚠️  HTML report not found"

                        fi                            --cov-report=html:reports/htmlcov \                        

                        

                        if [ -f reports/coverage.xml ]; then                            --cov-report=term-missing \                        echo "Installed packages:"

                            echo "✅ Coverage report generated"

                            ls -lh reports/coverage.xml                            || true                        pip freeze

                        else

                            echo "⚠️  Coverage report not found"                                            '''

                        fi

                    '''                        echo "Test execution completed"                }

                }

            }                    '''            }

        }

    }                }        }



    post {                echo '✅ Tests completed'        

        always {

            echo '📋 Pipeline execution completed'            }        stage('Start MySQL Container') {

            

            // Archive test reports        }            steps {

            dir('messaging_app') {

                // Publish JUnit test results                echo 'Starting MySQL database...'

                junit allowEmptyResults: true, testResults: 'reports/junit.xml'

                        stage('Generate Test Report') {                sh '''

                // Archive HTML reports

                archiveArtifacts artifacts: 'reports/**/*', allowEmptyArchive: true, fingerprint: true            steps {                    if command -v docker >/dev/null 2>&1; then

                

                // Publish HTML report                echo '📊 Generating test reports...'                        echo "Cleaning up any existing MySQL container..."

                publishHTML(target: [

                    allowMissing: true,                dir('messaging_app') {                        docker rm -f ci-mysql 2>/dev/null || true

                    alwaysLinkToLastBuild: true,

                    keepAll: true,                    sh '''                        

                    reportDir: 'reports',

                    reportFiles: 'report.html',                        if [ -f reports/junit.xml ]; then                        echo "Starting MySQL 8.0 container..."

                    reportName: 'Pytest HTML Report'

                ])                            echo "✅ JUnit XML report generated"                        docker run -d \

                

                // Publish coverage report                            ls -lh reports/junit.xml                            --name ci-mysql \

                publishHTML(target: [

                    allowMissing: true,                        else                            -p 3306:3306 \

                    alwaysLinkToLastBuild: true,

                    keepAll: true,                            echo "⚠️  JUnit XML report not found"                            -e MYSQL_DATABASE=${DATABASE_NAME} \

                    reportDir: 'reports/htmlcov',

                    reportFiles: 'index.html',                        fi                            -e MYSQL_USER=${DATABASE_USER} \

                    reportName: 'Coverage Report'

                ])                                                    -e MYSQL_PASSWORD=${DATABASE_PASSWORD} \

            }

        }                        if [ -f reports/report.html ]; then                            -e MYSQL_ROOT_PASSWORD=${DATABASE_PASSWORD} \

        

        success {                            echo "✅ HTML report generated"                            --health-cmd="mysqladmin ping -h 127.0.0.1 -u${DATABASE_USER} -p${DATABASE_PASSWORD}" \

            echo '✅ Pipeline completed successfully!'

        }                            ls -lh reports/report.html                            --health-interval=5s \

        

        failure {                        else                            --health-timeout=3s \

            echo '❌ Pipeline failed. Check the logs for details.'

        }                            echo "⚠️  HTML report not found"                            --health-retries=30 \

        

        unstable {                        fi                            mysql:8.0

            echo '⚠️  Pipeline completed with warnings.'

        }                                                

    }

}                        if [ -f reports/coverage.xml ]; then                        echo "Waiting for MySQL to be healthy..."


                            echo "✅ Coverage report generated"                        for i in $(seq 1 60); do

                            ls -lh reports/coverage.xml                            status=$(docker inspect --format='{{.State.Health.Status}}' ci-mysql 2>/dev/null || echo "starting")

                        else                            echo "Attempt $i/60: MySQL status = $status"

                            echo "⚠️  Coverage report not found"                            

                        fi                            if [ "$status" = "healthy" ]; then

                    '''                                echo "MySQL is ready and healthy!"

                }                                sleep 3

            }                                exit 0

        }                            fi

    }                            sleep 3

                        done

    post {                        

        always {                        echo "MySQL failed to become healthy within timeout"

            echo '📋 Pipeline execution completed'                        docker logs ci-mysql

                                    exit 1

            // Archive test reports                    else

            dir('messaging_app') {                        echo "Docker not available - skipping MySQL container setup"

                // Publish JUnit test results                    fi

                junit allowEmptyResults: true, testResults: 'reports/junit.xml'                '''

                            }

                // Archive HTML reports        }

                archiveArtifacts artifacts: 'reports/**/*', allowEmptyArchive: true, fingerprint: true        

                        stage('Run Migrations') {

                // Publish HTML report            steps {

                publishHTML(target: [                echo 'Running Django migrations...'

                    allowMissing: true,                dir('messaging_app') {

                    alwaysLinkToLastBuild: true,                    sh '''

                    keepAll: true,                        . ${VENV_DIR}/bin/activate

                    reportDir: 'reports',                        

                    reportFiles: 'report.html',                        echo "Checking database connectivity..."

                    reportName: 'Pytest HTML Report'                        python manage.py check --database default || echo "Database check warning (may be expected)"

                ])                        

                                        echo "Running migrations..."

                // Publish coverage report                        python manage.py migrate --noinput

                publishHTML(target: [                        

                    allowMissing: true,                        echo "Migrations completed"

                    alwaysLinkToLastBuild: true,                    '''

                    keepAll: true,                }

                    reportDir: 'reports/htmlcov',            }

                    reportFiles: 'index.html',        }

                    reportName: 'Coverage Report'        

                ])        stage('Run Tests') {

            }            steps {

        }                echo 'Running pytest test suite...'

                        dir('messaging_app') {

        success {                    sh '''

            echo '✅ Pipeline completed successfully!'                        . ${VENV_DIR}/bin/activate

        }                        

                                echo "Running tests with pytest..."

        failure {                        pytest --maxfail=1 --disable-warnings -q --junitxml=report.xml

            echo '❌ Pipeline failed. Check the logs for details.'                        

        }                        echo "Copying report to workspace root for archival..."

                                cp report.xml ../report.xml 2>/dev/null || true

        unstable {                        

            echo '⚠️  Pipeline completed with warnings.'                        echo "All tests completed!"

        }                    '''

    }                }

}            }

        }
    }
    
    post {
        always {
            echo 'Publishing test results...'
            junit allowEmptyResults: true, testResults: 'report.xml'
            
            script {
                sh '''
                    if command -v docker >/dev/null 2>&1; then
                        echo "Cleaning up MySQL container..."
                        docker rm -f ci-mysql 2>/dev/null || true
                        echo "Cleanup complete"
                    fi
                '''
            }
        }
        success {
            echo '✅ Pipeline completed successfully! All stages passed.'
        }
        failure {
            echo '❌ Pipeline failed - review the logs above to identify the issue'
        }
        cleanup {
            echo 'Cleaning up workspace artifacts...'
            cleanWs deleteDirs: true, patterns: [
                [pattern: 'messaging_app/venv/**', type: 'INCLUDE'],
                [pattern: 'messaging_app/**/__pycache__/**', type: 'INCLUDE'],
                [pattern: 'messaging_app/.pytest_cache/**', type: 'INCLUDE']
            ]
        }
    }
}